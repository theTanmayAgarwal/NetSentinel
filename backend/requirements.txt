# Base runtime — lightweight and fast to install (no heavy ML wheels).
# The application is fully functional with just these dependencies.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.17
PyYAML==6.0.2
numpy==2.1.3
reportlab==4.2.5

# Tests
pytest==8.3.4
httpx==0.28.1
